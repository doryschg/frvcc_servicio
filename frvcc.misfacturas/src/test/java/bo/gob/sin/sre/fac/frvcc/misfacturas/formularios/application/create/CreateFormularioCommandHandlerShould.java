package bo.gob.sin.sre.fac.frvcc.misfacturas.formularios.application.create;

import bo.gob.sin.sre.fac.frvcc.misfacturas.formularios.FormularioModuleUnitTestCase;


public class CreateFormularioCommandHandlerShould  extends FormularioModuleUnitTestCase {
    /*private CreateFormularioCommandHandler handler;
    @BeforeEach
    protected void setUp(){
        super.setUp();
        handler = new CreateFormularioCommandHandler(new FormularioCreator(repository, eventBus));
    }
    @Test
    void create_a_validate_formulario(){
       CreateFormularioCommand command = CreateFormularioCommandMother.random();
       Formulario formulario = FormularioMother.fromRequest(command);
       handler.handle(command);
       shouldHaveSaved(formulario);

    }*/
}
