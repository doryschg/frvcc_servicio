package bo.gob.sin.sre.fac.frvcc.misfacturas.compras;





public abstract class CompraModuleUnitTestCase{ /*extends UnitTestCase {
    protected CompraRepository repository;

    @BeforeEach
    protected void setUp() {
        super.setUp();

        repository = mock(CompraRepository.class);

    }

    public void shouldHaveSaved(Compra compra) {
        verify(repository,atLeastOnce()).guardar(compra);
    }

    public void shouldSearch(Compra compra) {
        Mockito.when(repository.findById(compra.id())).thenReturn(Optional.of(compra));
    }

    public void shouldMatching(Criteria criteria,List<Compra> compras) {

        Mockito.when(repository.matching(criteria).getContent()).thenReturn(compras);

    }*/


}
